<h1> New Player </h1>

<%= form_for(@player) do |f| %> 
  <%= f.hidden_field :team_id %>

  <%= render partial: 'layouts/errors', locals: {object:@player} %>

    <%= f.label :name %> 
    <%= f.text_area :name %> <br>
    <%= f.label :age %> 
    <%= f.number_field :age %> <br>

    <%= f.label :team_id, "Select a Team" %> 
    <%= f.collection_select :team_id, Team.all, :town, :coach %><br> 

    <%= f.submit %> 
<% end %> 