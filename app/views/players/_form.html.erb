<%= form_for(@player) do |f| %> 
  
  <% if @player.errors.any? %> 
    <div id= "error_explanation"> 
     <ul>
        <% @player.errors.full_messages do |message| %> 
           <li><%= message %></li>
        <% end %>
      </ul> 
    </div>
  <% end %> 
        
        <%= f.label :name %> 
        <%= f.text_area :name %> <br>
        <%= f.label :age %> 
        <%= f.number_field :age %> <br>
    
        <%= f.hidden_field :team_id %>
        <%= f.hidden_field :user_id %>  
    
        <%= f.submit %> 
    <% end %> 